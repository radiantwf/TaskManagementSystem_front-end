/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SigninUserInfoComponent } from './signin-user-info.component';

describe('Component: SigninUserInfo', () => {
  it('should create an instance', () => {
    let component = new SigninUserInfoComponent();
    expect(component).toBeTruthy();
  });
});
