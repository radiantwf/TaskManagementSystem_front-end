/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NullOrEmptyPipe } from './null-or-empty.pipe';

describe('Pipe: Null', () => {
  it('create an instance', () => {
    let pipe = new NullOrEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
