/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TextboxAutocomplete } from './textbox-autocomplete.directive';

describe('Directive: TextboxAutocomplete', () => {
  it('should create an instance', () => {
    let directive = new TextboxAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
