/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {MockTask} from './mock-task';

describe('MockTask', () => {
  it('should create an instance', () => {
    expect(new MockTask()).toBeTruthy();
  });
});
